# angular-standards

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build angular-standards` to build the library.

## Running unit tests

Run `nx test angular-standards` to execute the unit tests via [Jest](https://jestjs.io).
